{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    // Overrides the base DOM settings to isolate the service worker global scope
    "lib": ["ES2022", "WebWorker"], 
    "types": [],
    "noEmit": true
  },
  "include": ["sw.ts"]
}